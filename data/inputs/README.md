Raman spectral matrices are not included in the public submission package
because they contain commercially sensitive manufacturing spectral records.

The withheld files are:

- `LabelData_time.csv`
- `UnlabelRamanData.csv`

The non-spectral operation-time table `Batch_operation_times.csv` is retained
to document the process-time descriptor definitions used in the manuscript.
